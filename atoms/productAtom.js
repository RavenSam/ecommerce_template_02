import { atom } from "recoil"

export const viewProductState = atom({
   key: "viewProduct",
   default: {},
})

export const selectedVarientState = atom({
   key: "selectedVarient",
   default: {},
})
